class Server {
  final String id;
  final String name;
  final String ownerId;
  final DateTime createdAt;

  Server({
    required this.id,
    required this.name,
    required this.ownerId,
    required this.createdAt,
  });
}